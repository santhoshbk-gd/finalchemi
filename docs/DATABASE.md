# Database

`User` is implemented with name, unique email, password hash, education level, XP, and timestamps. The planned collection set is `elements`, `lessons`, `quizzes`, `quizResults`, `compounds`, `achievements`, `userProgress`, `experiments`, `chatHistories`, and `notes`. Keep reusable reference data seeded and user data indexed by `userId`.
